#include <stdio.h>
// THERE IS SOME ERROR IN THIS CODE
int main()
{
    int pass;
    printf("Either pass in maths and science or not?");

    scanf("%s", &pass);
    printf("You have entered %s in maths and science\n", &pass);
     if (pass = pass)
     {
         printf("You will be awarded.");
     }
    else
    {
        printf("You will never be awarded.");
    }

    return 0;
}

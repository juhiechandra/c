// 2. Extend the program by separating the odd and even marks into two separate arrays named as odd_marks and even_marks respectively and finally copied these two arrays into a third arrays named as odd_even_marks so that all the odds marks are stored before all the marks which are even.

#include <stdio.h>
extern int student_array[5] int max();
int min();

int main()
{
    int max();
    int min();
    int student_array[5];
    int i, sum, average;

    printf("\nEnter students marks in X subject:\n");
    for (i = 0; i < 5; i++)
    {
        printf("Enter marks of student [%d]: ", i + 1);
        scanf("%d", &student_array[i]);
    }
    // displaying marks in list
    printf("Marks are :\n");
    for (i = 0; i < 5; i++)
    {
        printf("%d\t", student_array[i]);
    }

